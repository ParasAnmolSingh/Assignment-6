def student_data(student_id=-3, student_name="", student_class=-4) :
   if student_id>=0:
     print (f"Student ID :-> {student_id}" )
   if student_name!="":
     print (f"Student Name :-> {student_name}" )
   if student_class!=-4:
      print (f"Student class :-> {student_class}")
